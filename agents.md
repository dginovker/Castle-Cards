While doing work, split up code files that are over 500 lines of code where possible
Do not try to write things in "extensible" ways. Do the easiest, least lines of code method possible.
Do not write "fallbacks" - Let code fail spectacularly. Use invariant frequently.
